{
  "name": "capitulo-diez-desafio",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.1.7",
    "parcel": "latest"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /apx-desafio-m5",
    "deploy": "gh-pages -d build ",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
