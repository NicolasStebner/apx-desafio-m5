{
  "name": "capitulo-diez-desafio",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel":"latest"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html",
    "deploy":"gh-pages -d ./dist",
    "build-and-deploy":"npm run build && npm run deploy"
  }
}
